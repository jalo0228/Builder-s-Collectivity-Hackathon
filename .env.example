# Local development
PORT=5173

# Postgres (via docker-compose.yml)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/builder_collectivity

# Optional: enable semantic search + auto-seeding via Gemini
# GEMINI_API_KEY=your_key_here
